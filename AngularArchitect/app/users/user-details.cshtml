
<div class="row">

    <div class="col-lg-12">
        <ng-form>
            <h2>User details</h2>
            <span ng-bind="vm.User.id"></span>
            <br />
            <input class="form-control" ng-model="vm.User.name" />

            <button type="button" class="btn btn-default" ng-click="vm.setUser({user:vm.User})">Save</button>
            
        </ng-form>
    </div>
</div>